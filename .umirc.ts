import { defineConfig } from 'dumi';

export default defineConfig({
  title: 'antd/charts',
  base: '/ant-design-charts/',
  publicPath: '/ant-design-charts/',
  // more config: https://d.umijs.org/config
});
