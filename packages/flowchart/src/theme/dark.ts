// 节点配置，目前仅支持 normal
const NodeConfig = {
  normal: {
    stroke: '#A2B1C3',
    fill: '#FFFFFF',
  },
};

const LabelConfig = {
  normal: {
    fill: '#000',
  },
};

export const DarkTheme = {
  NodeConfig,
  LabelConfig,
};
