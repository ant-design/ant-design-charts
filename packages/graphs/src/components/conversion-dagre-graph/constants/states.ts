import { ITEM_STATE } from '../types';

export const BASE_STATES: ITEM_STATE[] = [ITEM_STATE.Active, ITEM_STATE.Selected];

export const SIZE_IMPACT_STATES: ITEM_STATE[] = [ITEM_STATE.Active, ITEM_STATE.Selected];
