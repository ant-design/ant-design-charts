import React, { useState, useEffect } from 'react';
import { <%= chartName %> } from '@ant-design/charts';

const Demo<%= chartName %>: React.FC = () => {
  <%- chartContent.toString() %>
  return <<%= chartName %> {...config} />;
};

export default Demo<%= chartName %>;

