import { Context } from 'react';
import { StyledConfig, StyledThemeProvider, Theme } from "../types";
export declare let DEFAULT_THEME_PROVIDER: StyledThemeProvider;
export declare let DEFAULT_THEME_CONTEXT: Context<Theme>;
export declare const setupStyled: (config: StyledConfig) => void;
