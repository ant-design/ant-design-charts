import { DisplayObject } from '../../shapes';
import type { PoptipPosition } from './types';
/**
 * 计算不同方向的 poptip 在目标盒子上的位置，配置 POPTIP_STYLE 达到需要的 样式效果
 * @param position PoptipPosition
 * @returns { x: number, y: number }
 */
export declare function getPositionXY(clientX: number, clientY: number, target: HTMLElement | DisplayObject, position: PoptipPosition, arrowPointAtCenter?: boolean, follow?: boolean): [number, number];
/**
 * 获取全局唯一的 dom 元素
 */
export declare function getSingleTonElement(id: string): HTMLElement;
