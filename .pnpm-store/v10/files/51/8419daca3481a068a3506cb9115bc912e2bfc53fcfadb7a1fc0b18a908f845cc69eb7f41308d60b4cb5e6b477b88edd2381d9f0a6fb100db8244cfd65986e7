import { ArcAxis } from './axis';
/**
 * ArcAxis component bind to x scale.
 */
export const AxisArc = (options) => {
    return ArcAxis(options);
};
AxisArc.props = Object.assign(Object.assign({}, ArcAxis.props), { defaultPosition: 'left' });
//# sourceMappingURL=axisArc.js.map