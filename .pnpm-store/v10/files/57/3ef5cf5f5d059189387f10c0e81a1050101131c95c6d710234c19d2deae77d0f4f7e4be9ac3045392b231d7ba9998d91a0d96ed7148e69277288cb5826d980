import { Component } from '../../core';
import type { Group } from '../../shapes';
import type { CategoryOptions, CategoryStyleProps } from './types';
export type { CategoryOptions, CategoryStyleProps };
export declare class Category extends Component<CategoryStyleProps> {
    constructor(options: CategoryOptions);
    private titleGroup;
    private title;
    private itemsGroup;
    private items;
    private renderTitle;
    private renderItems;
    private adjustLayout;
    private get availableSpace();
    getBBox(): DOMRect;
    render(attributes: Required<CategoryStyleProps>, container: Group): void;
}
