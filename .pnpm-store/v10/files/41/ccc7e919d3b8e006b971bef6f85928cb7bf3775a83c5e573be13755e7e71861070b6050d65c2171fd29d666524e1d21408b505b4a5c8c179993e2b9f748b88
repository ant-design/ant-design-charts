import { HtmlConfig } from "../config/types";
export declare class HtmlPlugin {
    private config;
    constructor(config: HtmlConfig);
    generate(outputDir: string, assets: {
        js: string[];
        css: string[];
    }, globalPublicPath?: string): Promise<void>;
}
