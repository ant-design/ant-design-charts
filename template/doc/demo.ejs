import React from 'react';
import ReactDOM from 'react-dom';
import { <%= chartName %> <%= useG2 %> <%= useMeasureTextWidth %> } from '@ant-design/charts';

const Demo<%= chartName %> = () => {
  <%- chartContent.toString() %>
  return <<%= chartName %> {...config} />;
};

ReactDOM.render(<Demo<%= chartName %> {...config} />, document.getElementById('container'));